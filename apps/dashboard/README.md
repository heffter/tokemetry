# tokemetry dashboard

Vue 3 + TypeScript + ECharts single-page application. The SPA is scaffolded
and implemented in the dashboard phase (see the project PRD); until then this
directory only reserves the location in the workspace.

Planned views: Now (live limit gauges, burn rate), Blocks, Trends, Breakdowns,
Sessions, Machines, Alerts, Settings.
